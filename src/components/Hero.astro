<section class="hero section">
  <div class="container">
    <p class="greeting">Hi, my name is</p>
    <h1 class="name">Kelvin Fernandes</h1>
    <h2 class="tagline">Bridging hardware reliability with back-end precision</h2>
    <p class="desc">
      Computer Science student with hands-on IT support experience, now
      channeling my troubleshooting mindset into DevOps — building automated,
      reliable infrastructure that just works.
    </p>
    <div class="cta-group">
      <a href="#contact" class="btn-primary">Get in touch</a>
      <a href="#experience" class="btn-secondary">View experience</a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 64px;
  }

  .greeting {
    font-family: var(--font-mono);
    font-size: 15px;
    color: var(--accent);
    margin-bottom: 16px;
  }

  .name {
    font-size: 64px;
    font-weight: 700;
    letter-spacing: -1.5px;
    line-height: 1.1;
  }

  .tagline {
    font-size: 24px;
    font-weight: 400;
    color: var(--text-secondary);
    margin-top: 12px;
    max-width: 600px;
  }

  .desc {
    color: var(--text-muted);
    margin-top: 24px;
    max-width: 560px;
    font-size: 16px;
  }

  .cta-group {
    display: flex;
    gap: 16px;
    margin-top: 36px;
  }

  .btn-primary {
    font-family: var(--font-mono);
    font-size: 14px;
    padding: 12px 28px;
    background: var(--accent);
    color: #fff;
    border-radius: var(--radius);
    border: none;
    cursor: pointer;
    transition: all var(--transition);
  }

  .btn-primary:hover {
    background: #2563eb;
    color: #fff;
  }

  .btn-secondary {
    font-family: var(--font-mono);
    font-size: 14px;
    padding: 12px 28px;
    background: transparent;
    color: var(--accent);
    border: 1px solid var(--accent);
    border-radius: var(--radius);
    cursor: pointer;
    transition: all var(--transition);
  }

  .btn-secondary:hover {
    background: var(--accent-glow);
    color: var(--accent);
  }

  @media (max-width: 768px) {
    .name {
      font-size: 40px;
    }
    .tagline {
      font-size: 18px;
    }
    .cta-group {
      flex-direction: column;
    }
  }
</style>
